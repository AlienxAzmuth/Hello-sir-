```mermaid
flowchart TD
    A[Adverse Drug Reaction] --> B[Type A (Augmented)]
    A --> C[Type B (Bizarre)]
    A --> D[Type C (Chronic)]
    A --> E[Type D/E/F]
- **Explanation**:
   - `flowchart TD` defines a top-down flowchart.
   - `A --> B` creates an arrow from node A to node B.
   - `[Text]` inside each node sets the label.
